{% extends"layoutdelibstart.html.twig" %}
{% block body %}

    <!-- page content -->
    <div class="container-fluid">

        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header card-header-icon" data-background-color="blue">
                        <i class="material-icons">assignment</i>
                    </div>
                    <div class="card-content">
                        <h4 class="card-title"> DELIBERATION: <span style="font-style: italic; color: grey; font-size: 14px;"> {%if(app.session.get('idsesdelib')  =='SE1')%}  1<sup>ère</sup>SESSION {% endif %} {%if(app.session.get('idsesdelib')  =='SE2')%}  2<sup>ème</sup>SESSION {% endif %} &nbsp;&nbsp;{%if(app.session.get('semtypedelib')  =='IMPAIR')%}  SEMESTRES IMPAIRS {% endif %}&nbsp;&nbsp;{%if(app.session.get('semtypedelib')  =='PAIR')%}  SEMESTRES PAIRS {% endif %}</h4>
                        <table id="levelspec"  class="table table-striped table-bordered table-condensed">
                            <thead>
                            <tr>
                                <th>Niveau</th>
                                <th>Spécialité</th>
                                <th>Semestre</th>
                                <th>Déliberation</th>






                            </tr>
                            </thead>


                            <tbody>

                            </tbody>
                        </table>
                    </div>

                </div>
            </div>
        </div>



    </div>
    <!-- /page content -->
    <!-- /page content -->




{% endblock %}