{% extends 'layoutaverpedagores.html.twig' %}

{% block title %}SIGES|Saisie - Notes Examens{% endblock %}

{% block body %}
    <div class="container-fluid">

        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header card-header-icon" data-background-color="blue">
                        <i class="material-icons">keyboard</i>
                    </div>
                    <div class="card-content">
                        <h4 class="card-title"> Saisie de Notes d'Examens : <span style="font-style: italic; color: grey; font-size: 14px;"> {%if(app.session.get('idsesentrynex')  =='SE1')%}  1<sup>ère</sup>SESSION {% endif %} {%if(app.session.get('idsesentrynex')  =='SE2')%}  2<sup>ème</sup>SESSION {% endif %} &nbsp;&nbsp;{%if(app.session.get('semtypeentrynex')  =='IMPAIR')%}  SEMESTRES IMPAIRS {% endif %}&nbsp;&nbsp;{%if(app.session.get('semtypeentrynex')  =='PAIR')%}  SEMESTRES PAIRS {% endif %}</h4>
                        <table id="listspeclevel"  class="table table-striped table-bordered table-condensed">
                            <thead>
                            <tr>
                                <th>Niveau</th>
                                <th>Spécialité</th>
                                <th>Semestre</th>
                                <th>Nbre. ECUE Saisie</th>







                            </tr>
                            </thead>


                            <tbody>

                            </tbody>
                        </table>
                    </div>

                </div>
            </div>
        </div>



    </div>
{% endblock %}
